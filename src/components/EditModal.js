import Reack from 'react';
import { StyleSheet, TextInput, View, Button, Modal } from 'react-native';

export const EditMaodal = ({}) => {
  return (
    <Modal>
      <View>
        <TextInput />
        <Button title="Відмінити" />
      </View>
    </Modal>
  );
};
